"""Poll job: delta-sync mailboxes, upsert the ledger, run closure rules.

STUB — fails the suite.
"""
from __future__ import annotations


def run_poll(graph, store, now) -> dict:
    raise NotImplementedError("stub engine")
